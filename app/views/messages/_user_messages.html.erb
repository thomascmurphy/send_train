<% if defined?(messages) && messages.present? %>
  <% messages.each do |message| %>
    <%= render :partial => "messages/message", object: message %>
  <% end %>
<% else %>
  <p class="text-center">You don't seem to have any messages here</p>
<% end %>
