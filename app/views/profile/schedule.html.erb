<div class="row">
  <div class="col-sm-8">
    <h1><%= @user.smart_name %>'s Schedule</h1>
  </div>
</div>

<div class="event_list margin_top">
  <%= render partial: 'events/index', locals: {coach_viewing: true} %>
</div>

<!-- Modal -->
<div class="modal fade" id="event_modal" tabindex="-1" role="dialog" aria-labelledby="eventModal">

</div>
